import styled from '@emotion/styled';

export const useStyles = styled(() => {
    return{
        input: {
            color: "#9a9fa1",
            padding: "15px 15px",
            fontSize:"15px",
        },
        icon: {
            color: "#2b5278",
            cursor:"pointer",
        }
    };
});