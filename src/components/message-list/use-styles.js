import styled from '@emotion/styled';

export const useStyles = styled(() => {
    return{
        input: {
            color: "#f5f6f6",
            padding: "15px 15px",
            fontSize:"15px",
        },
        icon: {
            color: "#f5f6f6",
            cursor:"pointer",
        }
    };
});